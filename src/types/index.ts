export type Company = {
  title: string;
  thumbnail: string;
};

export type Perk = {
  title: string;
  description: string;
  image: string;
};

export type ProjectShowcase = {
  id: string;
  name: string;
  description: string;
  color: string;
  textColor: string;
  headingColor: string;
  image: string;
};

export type Solution = {
  title: string;
  description: string;
  icon: string;
};
